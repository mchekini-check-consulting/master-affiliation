# Pipeline du projet site-a : ne se déclenche que si site-a/ change sur main.
name: site-a

on:
  push:
    branches: [main]
    paths:
      - "site-a/**"
  workflow_dispatch:

jobs:
  deploy:
    uses: ./.github/workflows/_deploy-project.yml
    with:
      project: site-a
    secrets: inherit
