# qualiopilote.fr  ->  service "qualiopilote-front" (port interne 80)
# Le nginx interne du frontend proxifie lui-même /api vers le backend.

# HTTP : sert le challenge ACME (Let's Encrypt) et redirige le reste en HTTPS.
server {
    listen 80;
    server_name qualiopilote.fr;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

# HTTPS : terminaison TLS ici, le trafic interne vers le conteneur reste en HTTP.
server {
    listen 443 ssl;
    http2 on;
    server_name qualiopilote.fr;

    ssl_certificate     /etc/letsencrypt/live/qualiopilote.fr/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/qualiopilote.fr/privkey.pem;
    include /etc/nginx/snippets/ssl-params.conf;

    location / {
        # La variable force la résolution DNS à chaque requête :
        # nginx démarre même si cette app est arrêtée.
        set $upstream http://qualiopilote-front:80;
        proxy_pass $upstream;

        proxy_set_header Host              $host;
        proxy_set_header X-Real-IP         $remote_addr;
        proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_http_version 1.1;
        proxy_set_header Upgrade    $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}
